plugins {
    id 'com.android.library'
}

android {
    namespace "app.desktop.compatlib"

    buildFeatures {
        buildConfig true
        aidl true
    }

    sourceSets {
        main {
            aidl.srcDirs = ['src/main/java']
        }
    }
}

addFrameworkJar('framework-15.jar')
