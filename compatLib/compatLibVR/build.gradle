plugins {
    id 'com.android.library'
}

android {
    namespace "app.desktop.compatlib.eleven"
    buildFeatures {
        buildConfig true
    }
}

addFrameworkJar('framework-11.jar')

dependencies {
    api projects.compatLib.compatLibVQ
}
