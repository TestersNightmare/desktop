apply plugin: 'com.android.library'

android {
    namespace "com.android.app.search"
    buildFeatures {
        buildConfig true
    }
    sourceSets {

        main {
            java.srcDirs = ['src']
        }
    }
}
